
namespace RefactoredSnake {

	/// <summary>
	/// Pretty self explanatory... which is the point exactly,
	/// instead of using raw numbers like in the original version
	///  
	/// </summary>
	public enum Direction {
		Up, Right, Down, Left
	}
}
